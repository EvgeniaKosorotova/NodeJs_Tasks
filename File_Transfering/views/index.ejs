<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" href="#">
    <title>File-Transfering</title>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="/css/styles.css" />
  </head>

<body>
  <main>
      <form class="gap-2 text-center" action="/" method="POST" enctype="multipart/form-data">
        <h3>Upload files</h3>
          <input class="form-control" type="file" name="file" id="">
          <button class="btn btn-outline-secondary" type="submit">Upload</button>
      </form>

      <hr>

      <table class="table align-middle">
        <tbody>
          <% fileInfos.forEach(function(fileInfo) { %>
            <tr>
              <div>
                <td>
                  <span class="align-text-bottom"><%= fileInfo.name %></span>
                </td>
                <td>
                  <a href="<%= fileInfo.url %>" class="btn btn-outline-secondary">Download</a>
                </td>
                <td class='preview'>
                  <% if (fileInfo.isImage) { %>
                    <img class='preview' src="<%= fileInfo.url %>" alt="preview">
                  <% } %>
                </td>
              </div>
            </tr>
            <% }); %>
          </tbody>
        </table>
  </main>
</body>

</html>